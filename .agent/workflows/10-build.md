# Build Firmware (No Flash)

Goal: Build only, do not upload.

Steps:
1) Confirm PlatformIO is installed.
2) Run `pio --version`
3) Run `pio run`
4) Report:
   - success/failure
   - binary size summary
   - what libraries were downloaded
